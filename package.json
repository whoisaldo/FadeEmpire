{
  "name": "fade-empire",
  "version": "1.0.0",
  "private": true,
  "description": "Fade Empire barbershop — static site + Supabase booking. package.json exists only for the test tooling; the deployed site has no build step.",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:db": "supabase db start && supabase test db",
    "health": "node tests/ci/live-health.mjs",
    "serve": "python3 -m http.server 8010"
  },
  "devDependencies": {
    "jsdom": "^25.0.1",
    "vitest": "^2.1.8"
  }
}
