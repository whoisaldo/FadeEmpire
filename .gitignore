# macOS
.DS_Store

# Supabase CLI local cache (linked project ref, version pins, etc.)
supabase/.temp/

# Local dev tooling — secrets + exported customer data must never be committed
tools/.env.local
tools/*.local.*
tools/appointments.local.*

# Test tooling (vitest) — never deployed; GitHub Pages serves the repo as-is
node_modules/

# Supabase CLI local-stack artifacts
supabase/.branches/
